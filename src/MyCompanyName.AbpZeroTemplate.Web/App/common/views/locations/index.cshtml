@using Taskever.Localization
<div ng-controller="common.views.location.index as vm">
    <div class="row margin-bottom-5">
        <div class="col-xs-12">
            <div class="page-head">
                <div class="page-title">
                    <h1>
                        <span>@*@L("LocationManage")*@物料管理</span> <small>物料类型</small>
                    </h1>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-4">
            <div class="portlet light">
                <div class="portlet-title">
                    <div class="caption">
                        <span class="caption-subject bold uppercase">
                            物料管理
                        </span>
                    </div>
                    @*<div class="actions">
                        <a ng-if="vm.permissions.manageOrganizationTree" ng-click="vm.organizationTree.addUnit(null)" class="btn btn-circle btn-default">
                            <i class="fa fa-plus"></i> @L("AddRootUnit")
                        </a>
                    </div>*@
                </div>
                <div class="portlet-body">
                    <div id="OrganizationUnitEditTree"></div>
                    <div ng-if="!vm.organizationTree.unitCount" class="text-muted">
                        @L("NoOrganizationUnitDefinedYet")
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-8">
            <div class="portlet light">
                <div class="portlet-title">
                    <div class="caption">
                        <span class="caption-subject bold uppercase">
                            @*@L("Members")*@物料列表
                            <span ng-if="vm.organizationTree.selectedOu.displayName">: {{vm.organizationTree.selectedOu.displayName}}</span>
                        </span>
                    </div>
                    <div class="actions" ng-if="vm.organizationTree.selectedOu.id">
                        <a  data-ui-sref="productEdited" class="btn btn-circle btn-default">
                            <i class="fa fa-plus"></i> 增加物料
                        </a>
                        @*<a ng-click="vm.getProduct(4)" class="btn btn-circle btn-default">
                            <i class="fa fa-plus"></i> 刷新物料
                        </a>*@
                    </div>
                </div>
                <div class="portlet-body">
                    <div ng-if="vm.organizationTree.selectedOu.id" ui-grid="vm.productGridOptions" ui-grid-pagination ui-grid-resize-columns dir=@(CultureHelper.IsRtl ? "rtl" : "")></div>
                    <div ng-if="!vm.organizationTree.selectedOu.id" class="text-muted">
                        @L("SelectAnOrganizationUnitToSeeMembers")
                    </div>
                </div>

                <div>{{vm.productList}}</div>
            </div>
        </div>
    </div>
</div>